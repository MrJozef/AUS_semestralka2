#include <iostream>
#include <fstream>
#include "../structures/heap_monitor.h" 
#include "uzemnaJednotka.h"

using namespace std;

int main()
{
	initHeapMonitor();

	/*UzemnaJednotka* jedna;
	UzemnaJednotka* dva;
	UzemnaJednotka* tri;

	fstream* subor = new fstream;
	subor->open(CESTA_K_DATAM, ios::in);

	jedna = new UzemnaJednotka(subor);
	dva = new UzemnaJednotka(subor);
	tri = new UzemnaJednotka(subor);

	subor->close();

	delete jedna;
	delete dva;
	delete tri;

	delete subor;*/
	return 0;
}